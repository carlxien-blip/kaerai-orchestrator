# canonical-sync per-session state (machine-local)
.claude/canonical-sync-state/

# settings.json backups created by install.sh
.claude/settings.json.bak.*

# live worker registry — your actual dispatched-worker entries are private/machine-local
# (the .example schema is tracked; the real file is not)
active-workers.json

# OS / editor cruft
.DS_Store
*.swp
*~

# local-only tracked-projects list (uncomment if you keep yours private)
# hooks/tracked-projects.txt
