# <PROJECT_NAME> first principles

> The single home of this project's operable decision rules for build workers.
> CLAUDE.md should `@import` this file. The authoritative strategy lives in the hub;
> this is the operable projection — reference, don't copy.
> canonical-sync injects this file to running sessions when it changes.

**WHY** — the first-principles goal this project exists to serve:
(one or two sentences a worker can sanity-check every change against)

**Decision gates — pass these before acting:**
1. (a concrete, checkable rule)
2. (a concrete, checkable rule)
3. (a concrete, checkable rule)

**Out of scope / never do:**
- (things a worker must not do without escalating)
